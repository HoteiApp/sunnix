import { classNames } from "primereact/utils";
import { ServiceCM } from "../../../models";
const HealthSun = ({ scm }: Props) => {
    return (
        <div>
            HEALTHSUN AUTHORIZATION
        </div>
    );
}
type Props = {
    scm: ServiceCM | undefined;
}
export { HealthSun };